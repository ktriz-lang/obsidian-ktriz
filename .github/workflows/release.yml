name: Release

# Obsidian Community Plugin Store convention: tags WITHOUT "v" prefix
# e.g. "0.1.0", NOT "v0.1.0"
on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: write
  # Required for actions/attest-build-provenance to sign the release assets
  # so users can cryptographically verify they were built from this repo.
  id-token: write
  attestations: write

jobs:
  release:
    name: Build and release
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build plugin (production)
        run: npm run build

      - name: Verify bundle size
        run: |
          SIZE=$(stat -c%s main.js 2>/dev/null || stat -f%z main.js)
          echo "main.js size: ${SIZE} bytes"
          if [ "$SIZE" -gt 100000 ]; then
            echo "ERROR: main.js > 100 KB — unexpected dependency bundled?"
            exit 1
          fi

      - name: Generate build provenance attestations
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            main.js
            manifest.json
            styles.css

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${GITHUB_REF_NAME}"

          # Create or update the release (idempotent re-run)
          if gh release view "$VERSION" &>/dev/null; then
            echo "Release $VERSION already exists — uploading assets"
            gh release upload "$VERSION" main.js manifest.json styles.css --clobber
          else
            gh release create "$VERSION" \
              --title "$VERSION" \
              --generate-notes \
              main.js manifest.json styles.css
          fi
